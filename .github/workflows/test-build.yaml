name: test-build

on:
  push:
    branches:
      - handlerww/tidb-filter


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: docker/login-action@v1
        with:
          registry: registry.cn-heyuan.aliyuncs.com
          username: ${{ secrets.ALI_DOCKER_USERNAME }}
          password: ${{ secrets.ALI_DOCKER_PASSWORD }}
      - run: |
          make dev-docker-image DOCKER_IMAGE_TAG=registry.cn-heyuan.aliyuncs.com/handlerww/cilium-dev:latest
          docker push registry.cn-heyuan.aliyuncs.com/handlerww/cilium-dev:latest